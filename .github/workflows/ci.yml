name: Native macOS
on: [push, pull_request]
jobs:
  test-and-build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: swift --version
      - run: swift test
      - run: bash scripts/build-app.sh
      - run: ditto -c -k --sequesterRsrc --keepParent 'build/Calibre Nova.app' build/Calibre-Nova.zip
      - uses: actions/upload-artifact@v4
        with:
          name: Calibre-Nova-macOS
          path: build/Calibre-Nova.zip
